{
  "name": "kaizen-worker",
  "version": "0.1.0",
  "private": true,
  "packageManager": "bun@1.2.5",
  "description": "Git job worker runs from backend (bun run --cwd ../backend worker). This package is a thin alias.",
  "scripts": {
    "dev": "bun run --cwd ../backend worker",
    "build": "echo \"worker binary is backend/src/worker-cli.ts\"",
    "start": "bun run --cwd ../backend worker"
  }
}
